If the EPN is drawn directly on a \glyph{Map} then
  \attrib{owning\_compartment} is not set. Interpretation is that  EPN
  belongs to an invisible default compartment.
  (Reference \ref{defn:EntityPoolNode})